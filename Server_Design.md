# ארכיטקטורת שרת ענן — משחק שחמט Real-Time

---

## 1. היקף המערכת

```
משתמשים רשומים .............. 100,000,000
פעילים בו-זמנית ..............  10,000,000
משחקים פעילים במקביל .........   5,000,000
מהלכים בשנייה ................   5,000,000
```

**עקרון-על:** זרימה חלקה וזמן תגובה נמוך קודמים לעקביות מוחלטת ולעלות.

**עקרון מנחה נוסף (לא ניתן למשא ומתן):** לא ה-client וולא ה-Gateway מחליטים על חוקי המשחק. ה-**GameEngine** שרץ בתוך Game Server Shard נשאר ה-single source of truth היחיד לכל מהלך — בדיוק כמו במימוש המקומי הקיים (`server/logic/`), רק שעכשיו הוא רץ בתוך shard אחד מיני רבים, לא כתהליך שרת בודד.

---

## 2. רכיבי המערכת

שישה רכיבים, כל אחד באחריות אחת בלבד — תואמים ישירות לדיאגרמה:

**א. API Gateway**
מטפל בכל מה שאינו זמן-אמת: **login**, **rooms** (יצירה/הצטרפות לפי קוד), **history** (היסטוריית משחקים), בקשות שידוך. REST/HTTP. חסר מצב — לא שומר שום דבר מעבר לבקשה הבודדת. פונה ל-Auth Service ול-Rooms API כשירותים פנימיים.

**ב. WebSocket Gateway**
מחזיק את החיבור החי מול הלקוח לכל משך המשחק: מקבל פקודות משחק (click/jump), דוחף עדכוני מצב (state updates) בזמן אמת. Async I/O, **ללא thread-per-client** — כדי לתמוך בעשרות אלפי חיבורים בו-זמנית לכל node בלי לצרוך thread לכל חיבור. חסר לוגיקת משחק לחלוטין: מעביר הודעות בין הלקוח ל-NATS/Game Server Shard בלבד.

**ג. Matchmaker**
משדך שחקנים לזוגות לפי דירוג ותור המתנה (הנתונים ב-Redis, לא בזיכרון מקומי — כדי שהשירות עצמו יהיה חסר מצב וניתן ל-scale אופקי). לאחר שידוך, מבקש מה-Game Allocator להקצות shard למשחק החדש.

**ד. Game Allocator**
מחליט **באיזה Game Server Shard ירוץ כל room** — לפי עומס נוכחי, זמינות, ואזור גיאוגרפי של השחקנים. שירות בקרה (control plane) בלבד — לא נמצא בנתיב החם של המהלכים עצמם, רק בהקמת המשחק.

**ה. Game Server Shards**
מריצים את המשחקים בפועל: כל shard מחזיק **הרבה חדרים פעילים** (many active rooms) ותורי פקודות לכל חדר (room command queues), ומריץ בתוכו **GameEngine authoritative** — אותו מנוע חוקיות שקיים היום ב-`server/logic/` (Controller, GameState, RulesEngine וכו'). shard = תהליך ארוך-חיים שמריץ עשרות אלפי `GameMatch` קלים, לא container נפרד למשחק.

**ו. Observability**
logs, metrics, alerts, traces, load tests — לא רכיב "עסקי" אלא תשתית חוצה-כל-הרכיבים. בלעדיו אי אפשר לדעת אם המערכת בריאה בקנה מידה של מיליוני שחקנים.

**רכיב ניהול אופציונלי — Agones**
מנהל fleet אופציונלי מעל Kubernetes שמתמחה בניהול תהליכי משחק ארוכי-חיים (game server shards): מחליט מתי להקים/להוריד shard, עוקב אחרי בריאותו, ומאפשר scale-down מבוקר (לא לזרוק shard עם משחקים חיים באמצע). לא חובה לשלב הראשון (Docker Compose / K3s בסיסי), רלוונטי בעיקר בקנה מידה מלא.

---

## 3. פריסה — Kubernetes / K3s Cluster אזורי

```
              DNS / GSLB  —  ניתוב גיאוגרפי
                        |
        +---------------+---------------+
        |               |               |
   Region: US       Region: EU       Region: APAC
   (K8s/K3s Cluster) (K8s/K3s Cluster) (K8s/K3s Cluster)
```

כל אזור הוא cluster עצמאי (כמו בדיאגרמה) שמכיל את כל שישה הרכיבים + NATS + Agones (אופציונלי) + חיבור למסדי הנתונים. **בקנה מידה גדול, כל האדריכלות הזו חוזרת על עצמה על פני כמה אזורים** — לא רק שכפול קריאה, אלא cluster מלא ועצמאי לכל אזור, עם ניתוב גיאוגרפי ברמת ה-DNS שמפנה כל משתמש לאזור הקרוב אליו.

בתוך אזור בודד:

```
Clients → API Gateway (REST/HTTP)  → Auth Service, Rooms API
        → WS Gateway (WebSocket)   → NATS Event Bus
                                         |
                        +----------------+----------------+
                        |                |                |
                   Matchmaker      Game Allocator    Game Server Shards
                        |                |                | (GameEngine, many rooms)
                        +----------------+----------------+
                                         |
                        +----------------+----------------+
                        |                |                |
                  PostgreSQL          Redis          Observability
             (users, games,      (sessions, active   (logs, metrics,
              results, history)   rooms, reconnect,    alerts, traces,
                                   matchmaking queue)   load tests)
```

הקווים המקווקוים בדיאגרמה המקורית (Agones↔Game Server Shards, שכבת Observability) הם נתיבי בקרה/ניטור — לא זרימת דאטה של המשחק עצמו.

---

## 4. תקשורת

### לקוח מול שרת — שני ערוצים נפרדים בכוונה

**REST / HTTP → API Gateway:** login, rooms, history, בקשות שידוך. לא זמן-אמת, לא דורש חיבור פתוח.

**WebSocket → WS Gateway:** פקודות משחק חיות (click/jump) ועדכוני מצב. חיבור פתוח ורציף שמאפשר לשרת לדחוף עדכונים מיידית, ללא תקורת HTTP בכל מהלך.

הפרדה זו (ולא WebSocket יחיד לכל דבר, כמו במימוש המקומי הנוכחי) מאפשרת ל-API Gateway להיות **stateless לגמרי** ו-scale בנפרד מ-WS Gateway, שהוא הרכיב היחיד שבאמת צריך להחזיק חיבור-פר-משתמש.

### שרת מול שרת — NATS Event Bus + קריאה ישירה

**דרך NATS (Pub/Sub לפי נושא/מזהה משחק):** מהלכים, אירועי לכידה/סיום-משחק, שידור לצופים, עדכוני נוכחות (disconnect/reconnect). זרם מתמשך שאינו מחייב המתנה לתשובה — עומס בצד אחד אינו חוסם את שאר השרשרת.

**דרך קריאה ישירה (REST פנימי):** הקצאת shard חדש ע"י Game Allocator, בירור "איפה נמצא המשחק הזה" ע"י WS Gateway בזמן reconnect — שאילתה נקודתית שתוצאתה נחוצה מיד.

**חלופה:** Redis Pub/Sub יכול לשמש כתחליף קל-משקל ל-NATS בשלב הראשון (יש כבר Redis בארכיטקטורה לצרכים אחרים) — אך NATS מומלץ לטווח הארוך בזכות JetStream (persistence + at-least-once delivery מובנה), שחסר ב-Redis Pub/Sub הבסיסי.

---

## 5. ניתוב שחקנים — מי נמצא איפה

מפת ניתוב משותפת ב-**Redis**:

```
user_id  →  ws_gateway_node   (על איזה Gateway יושב החיבור החי)
room_id  →  game_server_shard (על איזה shard רץ החדר)
```

```
שחקן A ── WS Gateway #37  ──┐
                             ├──▶  room_id = X  ──▶  Game Server Shard #82
שחקן B ── WS Gateway #512 ──┘
```

הכתובת האמיתית במערכת היא **מזהה החדר (room_id)**, לא זהות ה-Gateway. הצטרפות לחדר היא הרשמה לנושא NATS של אותו room_id — כל שחקן יכול לשחק מול כל שחקן ולהיכנס לכל חדר, ללא תלות בשרת שאליו הוא מחובר בפועל. זהה בעיקרון למנגנון שכבר קיים היום ב-`GameRegistry` (username → GameId, RoomCode → GameId) — רק שהמיפוי עובר מזיכרון תהליך בודד ל-Redis משותף.

---

## 6. מסדי נתונים — חלוקה לפי אופי הנתון

| נתון | מקום | סיבה |
|---|---|---|
| מצב משחק פעיל, תורי פקודות | זיכרון ה-shard | חם, נגיש בכל מהלך, לא שווה תקורת רשת |
| Sessions, active rooms, reconnect, matchmaking queue | **Redis** | זמני, קצר-חיים, קריאה/כתיבה בקצב גבוה מאוד |
| Users, games, results, move history | **PostgreSQL** | קבוע, דורש עסקאות (transactions) אמיתיות — יצירת חשבון, עדכון דירוג, שמירת תוצאה |

**למה PostgreSQL ולא NoSQL מבוזר (כפי שהוצע בטיוטה קודמת של מסמך זה):** ההנחיה מ-CTD קובעת PostgreSQL + Redis כזוג הבסיס. PostgreSQL נותן עסקאות אמיתיות (חשוב לעדכון דירוג ELO אטומי, בדיוק כמו `RatingUpdater` הקיים היום), וב-100 מיליון משתמשים רשומים (לא 100 מיליון *כתיבות בשנייה* — הכתיבות התכופות הן ב-Redis) sharding מנוהל של Postgres (למשל לפי טווח/hash של user_id) מספיק. אם בעתיד קצב הכתיבה ל-history יהפוך לצוואר בקבוק, ניתן להעביר את היסטוריית המהלכים בלבד לאחסון עמודות/אובייקטים נפרד — אבל זו החלטה נדחית, לא נדרשת ביום הראשון.

**עקביות:** לכל room יש shard אחד בלבד שהוא מקור האמת — שני השחקנים תמיד רואים לוח זהה כי מדובר בשידור מהשרת, לא סנכרון בין עותקים. עקביות סופית (eventual consistency) חלה רק על דירוגים/לוחות תוצאות ב-PostgreSQL, שם עיכוב של שניות אינו מורגש.

---

## 7. עומס תעבורה (הערכה)

```
5,000,000 מהלכים לשנייה  ×  150 בייט  ×  2 כיוונים
                                    ≈ 1.5 GB לשנייה  ≈ 12 Gbps
                       ובתוספת הצפנה (WSS) ≈ 14–16 Gbps
```

מחולק על פני מאות WS Gateway nodes (חלוקה לפי `20,000 חיבורים לנוד` בערך → 500+ nodes ל-10M מחוברים בו-זמנית, עם רדונדנטיות).

**נושאי NATS** (או partitions ב-Redis Pub/Sub) מחולקים לפי `room_id`, כך שכל Game Server Shard קורא רק את ההודעות של החדרים שהוא עצמו מריץ.

**המסקנה כמו במסמך הקודם:** האתגר הוא קצב ההודעות (מיליוני הודעות קטנות בשנייה), לא רוחב הפס עצמו — ומכאן החשיבות של autoscaling אמיתי ב-Kubernetes, לא רק עוד "שרת חזק".

---

## 8. תרחישי כשל

| כשל | תוצאה | זמן השפעה |
|---|---|---|
| **WS Gateway node נופל** | הלקוח מזהה ניתוק, מתחבר מחדש ל-node אחר דרך ה-LB; שולף מ-Redis את `room_id` שלו וממשיך | ~שנייה |
| **Game Server Shard נופל** | Agones/Kubernetes מזהה ומרים shard מחליף; המצב האחרון משוחזר מ-Redis (checkpoint א-סינכרוני) | אובדן אפשרי של המהלך האחרון בלבד |
| **Matchmaker instance נופל** | מספר עותקים פעילים; תור ההמתנה שמור ב-Redis ולא בזיכרון מקומי — עותק אחר ממשיך | אין |
| **Game Allocator נופל** | לא בנתיב החם; משחקים קיימים ממשיכים לרוץ ב-shard שכבר הוקצה להם. רק הקצאות *חדשות* נחסמות עד להחלמה | השהיה בפתיחת משחקים חדשים בלבד |
| **NATS node נופל** | קלאסטר NATS עם quorum (≥3 nodes); node בודד לא מפיל את הצנרת | אין, אם מוגדר cluster |
| **PostgreSQL נופל** | המשחק ממשיך לרוץ מהזיכרון ומ-Redis; רק כתיבת history/rating נדחית לתור ותיכתב כשהמסד יחזור | אין בזמן אמת |
| **Redis נופל** | הבעיה החמורה ביותר — sessions/routing map/matchmaking queue נמצאים שם. נדרש Redis Cluster עם replication, לא instance בודד | תלוי בזמינות ה-cluster |
| **אזור שלם נופל** | GSLB מפנה משתמשים חדשים לאזור אחר; משתמשים קיימים באזור הנופל מתנתקים ומתחברים מחדש דרך ה-DNS לאזור הבא | ניתוק עד רענון DNS |

**כלל מנחה:** כל רכיב מלבד Game Server Shards, PostgreSQL ו-Redis חסר מצב לחלוטין — ולכן ניתן להחליף/להרים מחדש מיידית בלי אובדן מידע. שלושת אלה הם היחידים שדורשים אסטרטגיית שרידות ייעודית (checkpointing, replication).

---

## 9. גיבוי ועמידות

**PostgreSQL:** replication סינכרוני לפחות לעותק אחד נוסף (לא לאבד עסקה שכבר אושרה), + WAL archiving לשחזור עד לרגע התקלה, + snapshot תקופתי חוצה-אזורים.

**Redis:** Redis Cluster עם replication; מכיוון שהתוכן שם זמני-מטבעו (session/reconnect/queue), אובדן חלקי בנפילה נדיר הוא לא אסון — לכתובות הניתוב יש לוגיקת שחזור (Gateway יכול לשאול מחדש את ה-shard "מי נמצא איפה" אם הרשומה נעלמה).

**Game Server Shard state:** לא מקור אמת קבוע — checkpoint א-סינכרוני ל-Redis, לא ל-PostgreSQL (תדירות גבוהה מדי בשביל מסד עסקאות).

---

## 10. פריסה בפועל — מקומי מול ייצור

**מקומי / פיתוח — Docker Compose:** כל שישה הרכיבים + NATS + Redis + PostgreSQL כ-containers בקובץ `docker-compose.yml` אחד, מריצים בקנה מידה קטן (shard בודד, gateway בודד) על מחשב מפתח בודד. זו הדרך לפתח ולבדוק את חוזי ה-API בין הרכיבים בלי תלות בענן.

**ייצור — Kubernetes / K3s:** כל רכיב הוא Deployment/StatefulSet נפרד עם autoscaling (HPA) לפי המדד הרלוונטי לו (WS Gateway לפי מספר חיבורים, Game Server Shards לפי מספר חדרים פעילים). Game Server Shards, בהיותם stateful וארוכי-חיים, מנוהלים דרך Agones (אופציונלי) שיודע "לא להרוג" shard עם משחקים חיים באמצע scale-down.

---

## 11. Observability

**Logs:** מרוכזים (לא קובץ מקומי לכל node, כמו `server.log`/`client.log` היום) — נדרש aggregator (למשל Loki/ELK).

**Metrics:** קצב מהלכים, זמן שידוך ממוצע, אחוז תפוסת shard, latency בין click לתגובה — נחוצים כדי להזין את מדיניות ה-autoscaling, לא רק לצפייה.

**Alerts:** על סף (70/85/95% ניצולת דיסק/CPU, כמו במדיניות המקורית), על shard שלא שולח heartbeat, על תור NATS שמצטבר.

**Traces:** מעקב distributed אחרי מהלך בודד מ-WS Gateway דרך NATS עד ה-shard וחזרה — חיוני לאבחון latency בארכיטקטורה מבוזרת שבה אי אפשר יותר "לשים breakpoint".

**Load tests:** לא חד-פעמי — חלק קבוע מה-CI/CD, מדמה עשרות אלפי חיבורים כדי לתפוס רגרסיות ביצועים לפני ייצור.

---

## 12. התאמה לדרישות

**100 מיליון משתמשים רשומים** — PostgreSQL עם sharding מנוהל לפי user_id.

**10 מיליון פעילים בו-זמנית** — מאות WS Gateway nodes, stateless, autoscale לפי מספר חיבורים.

**חוקי משחק אמינים** — GameEngine authoritative רץ אך ורק בתוך Game Server Shard; לא ב-client, לא ב-Gateway.

**כולם משחקים עם כולם** — ניתוב לפי room_id דרך NATS/Redis, מנותק לחלוטין מזהות ה-Gateway שאליו כל שחקן מחובר בפועל.

**זרימה חלקה** — WebSocket מתמשך, NATS א-סינכרוני, מצב בזיכרון ה-shard, ללא חסימות בנתיב המשחק.

**שהמערכת לא תיתקע** — הפרדת שכבות, כל רכיב מלבד השלושה הקריטיים (Shard/Postgres/Redis) חסר מצב, autoscaling ובדיקות תקינות בכל שכבה.

**התאוששות מכשל** — Agones/Kubernetes מרימים shard מחדש עם checkpoint מ-Redis; שאר הרכיבים מוחלפים מיידית ללא אובדן מידע.
