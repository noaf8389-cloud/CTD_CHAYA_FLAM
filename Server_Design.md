# ארכיטקטורת שרת ענן — משחק שחמט Real-Time

---

## 1. היקף המערכת

```
משתמשים רשומים .............. 100,000,000
פעילים בו-זמנית ..............  10,000,000
משחקים פעילים במקביל .........   5,000,000
מהלכים בשנייה ................   5,000,000
משחקים נפתחים/מסתיימים .......      83,000 לשנייה
שחקנים הממתינים לשידוך .......     166,000 לשנייה
```

**עקרון-על:** זרימה חלקה וזמן תגובה נמוך קודמים לעקביות מוחלטת ולעלות.

---

## 2. שכבות השרתים

ארבעה סוגי שרתים, כל אחד באחריות אחת בלבד:

**א. שרתי קצה — Gateway**
מחזיקים את חיבור ה-WebSocket מול הלקוח, מבצעים אימות והגבלת קצב, ומעבירים הודעות בלבד. אינם מכילים לוגיקת משחק ואינם שומרים מצב.
כמות: 600–900 שרתים.

**ב. שירות שידוך — Matchmaking**
משדך שחקנים לזוגות לפי דירוג, אזור וזמן המתנה, ומקצה מזהה משחק. חסר מצב — תור הממתינים נשמר ב-Redis.
כמות: 20–50 שרתים.

**ג. שרתי משחק — Game Session**
מקור האמת של המשחק: בדיקת חוקיות מהלך, ניהול הלוח והטיימרים, ושידור לשני הצדדים. מחזיקים את המצב בזיכרון.
כמות: 150–200 שרתים.

**ד. שכבת שמירה — Persistence**
כותבת באופן א-סינכרוני למסד הנתונים: היסטוריה, תוצאות ועדכוני דירוג. חסרת מצב.
כמות: 50–100 שרתים.

```
בסיס החישוב:
  20,000 חיבורים לשרת קצה   →  10M / 20K  = 500  (+ רדונדנטיות)
  50,000 משחקים לשרת משחק   →   5M / 50K  = 100  (+ רדונדנטיות)
```

**מדוע ההפרדה נדרשת:** כל שכבה גדלה לפי פרמטר אחר — הקצה לפי מספר חיבורים, השידוך לפי קצב בקשות, שרתי המשחק לפי מספר משחקים פעילים. ההפרדה מאפשרת להרחיב כל שכבה בנפרד, ומשאירה את שרתי הקצה חסרי מצב — כך שנפילתם אינה גוררת אובדן נתונים.

**עיצוב הקונטיינרים:** כל קונטיינר הוא תהליך ארוך-חיים המנהל בתוכו עשרות אלפי אובייקטי משחק קלים. בקצב של 83,000 משחקים חדשים בשנייה, הקמת קונטיינר נפרד לכל משחק אינה ישימה.

---

## 3. ניתוב עומסים — מבנה היררכי דו-שכבתי

```
              DNS / GSLB  —  ניתוב גיאוגרפי
                        |
        +---------------+---------------+
        |               |               |
     LB (US)         LB (EU)        LB (APAC)
        |               |               |
    Gateways        Gateways        Gateways
```

**שכבה גיאוגרפית:** מפנה כל משתמש לאזור הקרוב אליו — מצמצמת זמן תגובה ומפזרת עומס בין יבשות.

**שכבה אזורית:** לפחות שני נתבים לכל אזור בתצורת Active-Active, עם בדיקות תקינות שמסירות נתב כושל אוטומטית.

---

## 4. תקשורת

### לקוח מול שרת
**WebSocket** — חיבור פתוח ורציף שמאפשר לשרת לדחוף עדכונים מיידית, ללא תקורת HTTP בכל מהלך. מתאים לקצב של מהלך כל שתי שניות.

### שרת מול שרת — מודל היברידי

**דרך תור הודעות (Message Queue / Pub-Sub לפי מזהה משחק):**
מהלכים, כניעה והצעת תיקו, ושידור לצופים.

**דרך קריאה ישירה (REST / gRPC):**
הקמת משחק חדש, התחברות מחדש וקבלת מצב הלוח, הצטרפות צופה, ואיתור שרת לפי מזהה משחק.

**הכלל המנחה:** תור משמש לזרם מתמשך שאינו מחייב המתנה לתשובה — כך שעומס בצד אחד אינו חוסם את שאר השרשרת. קריאה ישירה משמשת לשאילתה נקודתית שתוצאתה נחוצה מיד כדי להמשיך.

**אישור קבלה (ACK):** שייך לתור בלבד ומבטיח עיבוד של לפחות פעם אחת — הודעה שלא עובדה חוזרת לתור ומטופלת בידי שרת אחר.

---

## 5. ניתוב שחקנים — מי נמצא איפה

מפת ניתוב משותפת ב-Redis:

```
user_id  →  gateway_node
game_id  →  game_session_server
```

```
שחקן A ── Gateway #37  ──┐
                          ├──▶  game-id = X  ──▶  Game Session #82
שחקן B ── Gateway #512 ──┘
```

הכתובת האמיתית במערכת היא **מזהה המשחק**, לא זהות השרת. כניסה לחדר היא הרשמה לערוץ של אותו מזהה — ולכן כל שחקן יכול לשחק מול כל שחקן ולהיכנס לכל חדר, ללא תלות בשרת שאליו הוא מחובר בפועל.

---

## 6. עמידה בנפח התעבורה

```
5,000,000 מהלכים לשנייה  ×  150 בייט  ×  2 כיוונים
                                    = 1.5 GB לשנייה
                                    ≈ 12 Gbps
                       ובתוספת הצפנה ≈ 14–16 Gbps
```

לאחר פיזור על פני כ-700 שרתי קצה:

```
רוחב פס לשרת ......  ~23 Mbps      (מול כרטיס רשת של 1–25 Gbps)
הודעות לשרת ....... 7,000–10,000 לשנייה
```

**תור ההודעות** מחולק למחיצות לפי מזהה משחק, במספר התואם את מספר שרתי המשחק, כך שכל שרת קורא רק את ההודעות הרלוונטיות לו.

**המסקנה:** האתגר אינו רוחב הפס אלא קצב ההודעות. חמישה מיליון הודעות קטנות בשנייה מעמיסות על המעבד ועל ניהול החיבורים, לא על הצנרת — ומכאן הצורך בפיזור על מאות שרתים.

**הסתייגות:** החישוב מניח פיזור אחיד, ולכן נדרשת הרחבה אוטומטית דינמית לספיגת שעות שיא אזוריות.

---

## 7. מסד נתונים

**הבחירה: מסד NoSQL מבוזר** (Cassandra / DynamoDB / ScyllaDB) — חלוקה לרסיסים ושכפול מובנים, המאפשרים מיליוני כתיבות בשנייה ללא כותב מרכזי. נתוני רישום ואימות נשמרים באותה משפחה, עם כתיבות מותנות המבטיחות עקביות חזקה.

**חלוקת הנתונים:**
- מצב משחק פעיל — Redis, בזיכרון
- פרופילים ודירוגים — מסד NoSQL מבוזר
- היסטוריית מהלכים — מסד NoSQL או אחסון אובייקטים

**מודל העקביות: זמינות ועקביות סופית.** לכל משחק יש שרת אחד בלבד שהוא מקור האמת, ולכן שני השחקנים תמיד רואים לוח זהה — מדובר בשידור מהשרת ולא בסנכרון בין עותקים. עקביות סופית חלה רק על דירוגים ולוחות תוצאות, שם עיכוב של שניות אינו מורגש ומאפשר ביצועים גבוהים משמעותית.

---

## 8. תרחישי כשל

**נפילת שרת קצה**
החיבורים נשברים, הלקוח מזהה ומתחבר מחדש דרך נתב העומסים לשרת אחר. מצב המשחק לא נשמר בשרת זה, ולכן המשחק נמשך. השפעה: ניתוק של שנייה.

**נפילת שרת משחק**
שרת אחר מאמץ את המשחק וטוען את המצב האחרון מ-Redis. השפעה: אובדן אפשרי של המהלך האחרון בלבד.

**נפילת שירות שידוך**
מספר עותקים פעילים בכל רגע; תור הממתינים שמור ב-Redis ושורד. השפעה: אין.

**נפילת נתב עומסים**
לפחות שניים בכל אזור; בדיקת תקינות מסירה את הכושל תוך שניות. השפעה: אין.

**נפילת מסד הנתונים**
המשחק ממשיך לרוץ מהזיכרון ומ-Redis; רק שמירת ההיסטוריה נדחית. השפעה: אין בזמן אמת.

### הגנת שרתי המשחק
המצב נשמר בזיכרון המקומי ונכתב ל-Redis באופן א-סינכרוני. המהלך אינו ממתין לכתיבה, ולכן אין תוספת השהיה לזרימה השוטפת. המחיר — אובדן אפשרי של מהלך אחרון בנפילה נדירה — מוגבל מטבעו, שכן משחק נמשך בממוצע כדקה בלבד.

### גילוי כשלים
כל שרת שולח אות חיים לרישום השירותים; שלוש החמצות רצופות מסירות אותו מהרוטציה אוטומטית. עם למעלה מ-900 שרתים, המטרה אינה מניעת נפילות אלא זיהוי מהיר והתאוששות אוטומטית.

---

## 9. גיבוי

**שכפול בזמן אמת** — שלושה עותקים לפחות, פרוסים על אזורי זמינות שונים. מספק מעבר אוטומטי תוך שניות בנפילת מכונה או דיסק, ומהווה את הבסיס לזמינות השוטפת.

**תצלום מצב תקופתי** — לאחסון קר, מאפשר חזרה לנקודת זמן שלפני תקלה לוגית. זהו תרחיש שהשכפול אינו מונע, שכן הוא משכפל גם את התקלה.

**יומן שינויים רציף** — משלים את התצלום ומאפשר שחזור מדויק עד לרגע התקלה.

**גיבוי חוצה-אזורים** — הגנה מפני אסון אזורי.

שני המנגנונים הראשונים משלימים זה את זה ואינם חלופיים. שניהם פועלים מחוץ לנתיב החם ואינם פוגעים בביצועי המשחק.

---

## 10. ניהול מקום בדיסק

**מניעה**
- התראות מדורגות בסף של 70%, 85% ו-95%
- הרחבה אוטומטית עם פיזור נתונים מחדש
- מדיניות שימור: היסטוריה מעל 90 יום עוברת לאחסון קר
- דחיסת נוטציית המהלכים — חיסכון של 70%–90%

**תגובה מדורגת**
- שרת בודד מלא: המסד המבוזר מפנה כתיבות לשרתים פנויים
- הקלאסטר בגבול: עצירת לוגים לא-קריטיים, הרחבה אוטומטית, התראת חירום
- המסד חסום לכתיבה: המשחק ממשיך מ-Redis, רק ההיסטוריה נדחית

---

## 11. התאמה לדרישות

**100 מיליון משתמשים רשומים** — מסד NoSQL מבוזר עם חלוקה אוטומטית לרסיסים.

**10 מיליון פעילים בו-זמנית** — 600 עד 900 שרתי קצה בפריסה גיאוגרפית.

**כולם משחקים עם כולם** — ניתוב לפי מזהה משחק דרך תור הודעות, מנותק לחלוטין מזהות השרת.

**נפח תעבורה** — כ-23 מגה-ביט לשנייה וכ-10,000 הודעות לשרת, הרבה מתחת לרוויה.

**זרימה חלקה** — חיבור מתמשך, תור א-סינכרוני ומצב בזיכרון, ללא חסימות בנתיב המשחק.

**שהשרתים לא ייתקעו** — הפרדת שכבות, תור כבולם זעזועים, הרחבה אוטומטית ובדיקות תקינות.

**התאוששות מכשל** — רוב השכבות חסרות מצב, שרתי המשחק מגובים ב-Redis, ורדונדנטיות בכל רמה.